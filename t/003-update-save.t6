
use v6.d;
use lib 'lib';
use lib 'app';

use models;
use Test;

plan 3;

%*ENV<DISABLE-SQL-LOG> = True;

my @errors;
my User $user;

$user = User.create({fname => 'Fred', lname => 'Dred'});
ok $user.is-valid;

$user.fname = 'John';
ok $user.save;
ok $user.fname eq 'John';
