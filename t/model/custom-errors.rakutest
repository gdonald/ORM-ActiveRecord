use v6.d;
use lib 'lib';
use lib 'specs/lib';
use Model::CustomErrors;
use Test;

plan 2;

%*ENV<DISABLE-SQL-LOG> = True;

my $user = User.build;
nok $user.is-valid;
ok $user.errors.fname eq 'fname is required';

User.destroy-all;
