use v6.d;
use lib 'lib';
use lib 'specs/lib';
use Models::User;
use Test;

plan 3;

%*ENV<DISABLE-SQL-LOG> = True;

my $user = User.create({fname => 'Fred'});
ok $user.is-valid;

$user.fname = 'John';
ok $user.save;
ok $user.fname eq 'John';

User.destroy-all;
